---
description: Kratos 后端纪律（proto / protohttp / service）
globs: backend/**/*
alwaysApply: false
---

# Kratos 后端纪律

> **更新：2026-05-29** · SSOT：[docs/dev/kratos-migration.md](../docs/dev/kratos-migration.md) · [backend/LAYOUT.md](../backend/LAYOUT.md)

## 当前状态

- 生产：`make moe-social` · HTTP `:8888` · 单进程
- 契约：`api/<domain>/v1/*.proto` → `make gen` → `openapi.yaml`
- 实现：`internal/server/protohttp/<domain>/` + `internal/service`

## 新接口

1. 改 `api/<domain>/v1/*.proto`（加 `google.api.http`）
2. `cd backend && make gen`
3. 实现 `internal/service` + `internal/server/protohttp`
4. `make check`

## 禁止

- 新路由进已删除的 `api/defs` 或 `backend/rpc`
- 手改 `*_pb.go` / `*_http.pb.go`
- `make gen-api` / `make gen-rpc`（已退役）
