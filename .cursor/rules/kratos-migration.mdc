---
description: Kratos hybrid migration rules for backend Moe domain
globs: backend/**/*
alwaysApply: false
---

# Kratos 混合迁移纪律（Moe 域）

| 进度 | 文档 | 验收 |
|------|------|------|
| Hybrid Moe 100% | `docs/dev/kratos-migration.md` | `make verify-moe-complete` |
| 纯 Kratos 100% | `docs/dev/kratos-pure-migration-plan.md` | `make verify-kratos-100` |

## 新功能（Moe）

1. 契约：`api/moe/v1/moe.proto`；配置 SSOT：`internal/conf/moe/v1/pilot.proto`。
2. 业务：`internal/biz|service|data/moe`。
3. Admin HTTP：`moeadmingw.NewConfigured`；试点：`api/moekratospilot`。
4. gRPC：`internal/server/moegrpc`。
5. 试点启动：`make moe-kratos`（Wire：`moekratos/wire_gen.go`）。

## 生成

```bash
make gen-moe-proto
make gen-moe-conf
make gen-moe-admin   # 改 super.api 后
```

## 运行

- 生产对外 **:8888**：`make moe-social` / `make build-moe-social`
- 试点（非对外）：`make moe-kratos`（:19031/:19032）

## VIP 试点（Phase 5）

- `internal/biz/vip`；试点 HTTP `/api/admin/vip/plans` @ :19032

## Legacy

User/VIP/社交仍 `super.api` / `super.proto`。
