---
description: Backend spec for Kratos Go API changes
globs: backend/**/*.go
alwaysApply: false
---

# 后端生成规范（Kratos）

在修改 `backend/` 下代码时，必须先阅读本规范，再开始改动。

## 修改前确认
- 详细规范以 `docs/guidelines/Codex启动指南-后端.md` 为准，开始改动前先读取该文档。
- 先确认需求是否只涉及后端；禁止把前端 UI 逻辑混入后端改动。
- 先读取并遵守本文件，不得跳过。
- 必须先阅读将被修改的现有后端文件（至少同目录相关 handler/service 文件），严格沿用当前项目最新代码风格与命名习惯。
- 统一规则先看 `.cursor/rules/moe-social-unified.mdc`；本文件只补充 `backend/` 细则。

## 实现要求
- 生产分层：`internal/server/protohttp` / `transport` → `internal/service` → `internal/biz` → `internal/data`。
- 新接口走 `api/<domain>/v1/*.proto` + `google.api.http` + `http_proto.go` 注册（见 `docs/dev/new-api-kratos.md`）。
- 勿往已退役的 `api/defs`、`backend/rpc`、go-zero logic 加新路由或业务。
- 请求参数校验必须完整，返回错误要有明确业务语义，不泄露内部细节。
- 新增/修改接口时，保证路由、handler、请求结构体、响应结构体一致。
- 涉及数据库或外部调用时，必须做错误处理和超时控制。
- 代码格式必须与现有 Go 文件保持一致，提交前执行 `gofmt`；避免改动自动生成文件中的无关内容。

## 提交前检查
- 仅修改 `backend/` 相关文件，不混入前端文件。
- 确认没有破坏现有接口契约（字段名、类型、状态码）。
- 确认关键路径有基础测试或最小可验证步骤。
- 若执行过 `make gen`：执行 `cd backend && make check`，并确认未手改生成文件。

## 管理台前端（moe-admin）
- 路径为 `moe-admin/`，读 `.cursor/rules/moe-admin-ai-spec.mdc` 与 `docs/guidelines/Codex启动指南-前端.md`（两套前端表）。
