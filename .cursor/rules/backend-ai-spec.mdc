---
description: Backend spec for Kratos Go API changes
globs: backend/**/*
alwaysApply: false
---

# 后端规范（Kratos）

修改 `backend/` 时自动加载。SSOT：`docs/dev/kratos-migration.md`、`backend/LAYOUT.md`、`docs/dev/new-api-kratos.md`。

## 结构

| 路径 | 角色 |
|------|------|
| `cmd/moe-social/` | 生产入口 |
| `api/<domain>/v1/*.proto` | 契约 |
| `internal/server/protohttp/` | HTTP 适配 |
| `internal/service` → `biz` → `data` | 分层 |
| `openapi.yaml` | Apifox |

## 新接口

1. 改 proto（`google.api.http`）→ `make gen`
2. 实现 service + protohttp → `make check`

## 禁止

- `api/defs`、`backend/rpc`、go-zero logic、`make gen-api` / `make gen-rpc`
- 手改 `*_pb.go` / `*_http.pb.go`

## 实现

- 先读同目录现有代码；完整校验；错误有业务语义
- `gofmt`；透传 `context.Context`
