---
description: Moe Social 仓库统一开发规则（Flutter / Kratos / Moe Admin）
alwaysApply: true
---

# Moe Social 统一规则（单一事实来源）

本文件是本仓库 Cursor 规则入口。工程规则细则见 `moe-social-engineering.mdc`。域细则由 glob 自动加载：`frontend-ai-spec`（`lib/`）、`backend-ai-spec`（`backend/`）、`moe-admin-ai-spec`（`moe-admin/`）。

## 1. 动手前

- 新增/修改接口、修 BUG、重构：先读现有实现与相关 `docs/dev/` SSOT，再改代码。
- **必读** [.cursor/LESSONS.md](../LESSONS.md)（<80 行）。
- 先明确边界、输入输出、验收标准。

## 2. 三栈边界

- Flutter → `lib/**`；后端 → `backend/**`；管理台 → `moe-admin/**`（React，非 Flutter）。
- 契约联动：先冻结 `backend/api/<domain>/v1/*.proto`，再改 UI。

## 3. 分层与 API

- 后端：`protohttp → service → biz → data`。
- Proto → `cd backend && make gen`；禁止手改 `*.pb.go`、`*_http.pb.go`、`openapi.yaml`。
- 迁移 SSOT：`docs/dev/kratos-migration-status.md`、`docs/dev/new-api-kratos.md`。

## 4. 质量门禁

- Flutter：`dart format`、`flutter analyze`
- 后端：`cd backend && make check`
- 管理台：`cd moe-admin && npm run build`
- 「完成」= 改动 + 检查已跑 + 结果写在回复里；无明确要求不 `commit` / `push`。

## 5. 大任务（可选）

≥5 文件或 ≥2 独立目录时：拆子任务、用 `Task` 工具并行、git worktree 隔离分支。共享文件由主会话最后合并。

## 6. 技能（仓库内）

Go 改动：`golang-style`、`effective-go`、`implementation-guardrails`、`golang-gin-database`。提交：`git-commit`。

## 7. 收尾

- 写清改了什么、检查是否通过、剩余风险。
- 重复踩坑只**提议**写入 `LESSONS.md`（需人确认）。
- 每次回复末尾带：`根据项目规则进行调整 喵～`
