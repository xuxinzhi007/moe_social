---
description: 大型任务用多子代理并行 + git worktree 隔离；日常小改单线程即可
alwaysApply: true
---

# 多 Agent 并行工作流（长期适用）

> 参考：Claude Code [parallel sessions with git worktrees](https://code.claude.com/docs/en/common-workflows#run-parallel-claude-code-sessions-with-git-worktrees)

## 何时必须并行

满足 **任意两条** 即视为「大任务」，**禁止**单会话硬扛到底：

- 预计改动 **≥5 个文件** 或 **≥2 个独立目录/域**
- 可拆成 **≥2 个无文件重叠** 的子任务（如 `user` / `admin` / `platform`）
- 需要 **调研 + 实现 + 文档** 在同一次交付里完成
- 用户明确说「很大 / 分波次 / 加速 / 多 agent」

**仍单线程**：单文件 bug、纯问答、<3 文件且同模块、强依赖顺序的链式重构。

## 父 Agent 怎么做

1. **先拆任务**（写清边界、成功标准、`make check` 范围），再动手写代码。
2. **对用户说明并行计划**（2～4 条即可），然后 **同一轮** 发起多个 `Task` 子代理（`run_in_background: true` 当子任务互不阻塞时）。
3. **按域选子代理**（示例）：
   - `backend-api-agent` / `explore`：后端 compat、proto、service
   - `flutter-ui-agent`：仅 `lib/pages` / `lib/widgets`
   - `code-reviewer-agent`：多子代理完成后统一 `make check` + 回归
4. **子代理输入必须自包含**：路径、禁止改动的文件、验收命令、与兄弟任务的接口约定（不要假设子代理读过本会话）。
5. **合并前**：父代理汇总 diff，解决冲突，**只跑一遍** `make check`（或域相关子集），更新文档一次。

对大任务可使用提示语：**「本任务较难，请拆分子任务并用多个 subagent 并行完成。」**

## Git：优先 worktree，不是多开同一目录

同一工作区同时改多域易冲突、易丢改动。并行会话用 **worktree + 分支**：

```bash
# 在仓库根目录
git worktree add ../moe_social-feat-admin -b feat/admin-app-compat
git worktree add ../moe_social-feat-user  -b feat/user-app-compat

# 各目录独立开 Cursor / Agent 会话
cd ../moe_social-feat-admin && # 仅改 admin_service_compat 等
cd ../moe_social-feat-user  && # 仅改 user_* compat 等
```

- **一域一分支一 worktree**；合并顺序：先合并底层/无依赖域，再合并有交叉的域。
- 合并后：`git worktree remove <path>`，删除已合并分支。

## 与本仓库结合的拆分示例（Kratos compat 迁移）

| 子代理 | 范围 | 避免触碰 |
|--------|------|----------|
| A | `admin_service_compat` → `AdminApp` | `user_*`、`platform_*` |
| B | `user_compat` / `user_memory_compat` → App | `admin_*` |
| C | `ai_compat` / `chat_compat` | `admin_service_compat.go` |
| D | `platform_compat` + `docs/dev/kratos-*` | 其它 `*_compat.go` |

共享文件（`register_all.go`、`route_stats.go`）由 **父代理最后单点修改**，不要多个子代理同时改。

## 纪律

- 子代理 **禁止** 扩大范围（不顺手重构无关模块）。
- 每个子任务结束必须报告：**改了什么、跑了什么命令、是否通过**。
- 文档与规则：大任务收尾时 **一次** 更新 SSOT（如 `docs/dev/kratos-legacy-api-migration.md`），避免四份互相矛盾的进度描述。
