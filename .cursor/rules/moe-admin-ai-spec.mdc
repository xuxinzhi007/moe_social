---
description: Admin console (moe-admin) React/TS UI spec
globs: moe-admin/**/*
alwaysApply: false
---

# 管理台规范（moe-admin）

React + Vite + TS。**不是** Flutter；勿套用 `lib/widgets` 约定。统一规则见 `moe-social-unified.mdc`。

## SSOT

1. `moe-admin/DESIGN.md`
2. `moe-admin/docs/ui-skeleton-v3.md`
3. `moe-admin/docs/css-architecture-v3.md`
4. `moe-admin/src/styles/index.css`

## 纪律

- 骨架：`moe-admin/src/ui/`（`ListPageLayout` / `TabbedPageLayout` / `MonitorPageLayout` / `PageHead`）
- 组件：`PageTabs`、`DataEnvBar`、`AdminIcon`、`AdminTag`
- API：`moe-admin/src/lib`；新接口先改 proto 并 `make gen`
- 禁止手写 hero 横幅；禁止往 `moe-admin-theme.css` 追加；颜色用 `tokens/colors.css`

## 提交前

`cd moe-admin && npm run build`
