---
description: Flutter UI spec — Moe design system and app structure
globs: lib/**/*.dart
alwaysApply: false
---

# Flutter 规范

修改 `lib/` 时自动加载。统一规则见 `moe-social-unified.mdc`。**管理台**在 `moe-admin/`，见 `moe-admin-ai-spec.mdc`。

## 目录

- `lib/pages/<domain>/` — 业务页面
- `lib/widgets/` — 复用组件
- `lib/services/`、`lib/models/`、`lib/providers/`
- `lib/app/app_routes.dart` — 路由；重页面用 `deferred import` + `DeferredRoute`

## Moe UI

- **主渐变** `#7F7FD5` → `#86A8E7`；**背景** `#F5F7FA`
- 白卡片、圆角 20/24、柔和紫阴影；`ElevatedButton` + 圆角/胶囊
- `MoeReveal` / `MoeStaggerReveal` / `MoeRevealOnce` 入场；`MoePressable` 按压反馈
- 长列表用 `MoeStaggerReveal` + `itemKey`/`revealedKeys` 避免刷新重播；Chat 历史加载后标记已见
- 礼物/成就粒子动效用 `MoeVfxProfile.fromContext` 适配 iOS/Android（安全区、触觉、粒子缩放）；Web 自动降级
- 禁止 Material/iOS 默认风、尖角、`ListTile` 做主菜单

## 编码

- **页面禁止直接 `ApiService`**：走 `UserService` / `PostService` / `ChatService` / `CommerceService` / `CommunityService` / `AuthFlowService` / `LifeService`；低层 URL/Header 用 `ApiClient`
- 网络经 `lib/services/`；`Provider` + `if (mounted)`；避免 `!`
- 新页面优先 **Provider/ChangeNotifier ViewModel**（参考 `GamePlayViewModel`、`CheckInProvider`），避免巨型 `setState`
- 接口变更先对齐 `backend/` 契约，再改 models/services/pages
- 充值等用户保留的演示功能不要顺手改真实链路
- `dart format` + `flutter analyze`；加载/成功/失败三态自测
- Agent 实现/审核：统一 skill **`moe-flutter`**（`.cursor/skills/moe-flutter/SKILL.md`）；支持 `audit` 只评不改

## 性能（已约定）

- `main()` 立即 `runApp`；Splash 只 await 关键初始化
- 扫码、VIP、通话等重页走路由懒加载
