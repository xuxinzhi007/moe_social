---
description: Compound Engineering 工作流 — 大任务用 Plan/Work/Review/Compound；小改单线程。详见 docs/guidelines/compound-engineering.md
alwaysApply: false
---

# Compound Engineering（按需）

> 详表：[docs/guidelines/compound-engineering.md](../../docs/guidelines/compound-engineering.md)  
> 插件：Cursor `compound-engineering`（`.cursor/settings.json`）  
> **首次使用**：在 Agent 聊天运行 `/ce-setup` 做环境诊断与本地配置引导。

## 何时采用（自决 ✅）

满足 **任意一条** 即按 CE 四步思考（Plan → Work → Review → Compound），即使用户未输入 `/ce-*`：

- 需求不清、多方案、或预计 **≥5 文件 / ≥2 独立目录**
- 跨 `lib/`、`backend/`、`moe-admin/` 契约联动
- 合并前需系统化审查（可建议 `/ce-code-review` 或 `code-reviewer-agent`）
- 本轮会产生可复用流程/架构知识

## 何时不采用（自决 ❌）

- 单文件 bug、纯问答、用户已给完整步骤
- 仅文档 typo / 链接修复
- 紧急热修（先交付，收尾再 compound）

## Agent 执行方式

1. **Plan**：读相关 SSOT（`kratos-migration-status.md`、`new-api-kratos.md`、产品优先级）；大任务先写简短计划再改代码。
2. **Work**：遵守 `parallel-agent-workflow.mdc`；验收 `make check` / `flutter analyze`。
3. **Review**：实质改动对照 `code_review.md`；可提示用户 `/ce-code-review`。
4. **Compound**：收尾用 [sessions/_TEMPLATE.md](../../docs/guidelines/sessions/_TEMPLATE.md)；重复踩坑只**提议**写入 `.cursor/LESSONS.md`。

用户在新会话中可显式调用：`/ce-brainstorm` → `/ce-plan` → `/ce-work` → `/ce-code-review` → `/ce-compound`（见指南文档）。

**插件装不上**：见 [compound-engineering.md](../../docs/guidelines/compound-engineering.md) §故障排查（清 `~/.cursor/plugins/cache` 或手动 symlink）；`.cursor/settings.json` 的 `enabled` 不能代替下载。

## 与本仓库硬规则的关系

CE **不覆盖**：`ai-startup-enforcer.mdc`、`agent-memory.mdc`、各 `*-ai-spec.mdc`。冲突时以仓库规范与 SSOT 为准。
