# 上传 bin/moe-social 后的二进制部署（见 deploy-agent backend_upload_binaries）
services:
  moe-social:
    image: alpine:latest
    container_name: moe-social
    working_dir: /app
    command: ["/app/moe-social", "-f", "config/config.yaml"]
    restart: always
    ports:
      - "8888:8888"
    volumes:
      - ./bin/moe-social:/app/moe-social
      - ./api/etc:/app/api/etc
      - ./config:/app/config
      - /root/gowork/moe_images:/app/data/images
