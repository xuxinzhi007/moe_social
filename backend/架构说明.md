# Backend 架构（简）

> **SSOT**：[LAYOUT.md](./LAYOUT.md) · [docs/dev/kratos-migration-status.md](../docs/dev/kratos-migration-status.md)

```text
make moe-social → cmd/moe-social → Kratos HTTP :8888
  → api/<domain>/v1/*.proto（契约 + openapi.yaml）
  → internal/server/grpc/<domain>/（proto HTTP 适配层，非 gRPC 监听）
  → internal/service → internal/biz → internal/data
```

历史 go-zero / api+rpc 双进程文档已迁至 `docs/archive/backend/`。
