# Backend 架构（简）

> **SSOT**：[LAYOUT.md](./LAYOUT.md)

```text
make moe-social → cmd/moe-social → Kratos HTTP :8888
  → api/<domain>/v1/*.proto
  → internal/server/protohttp/<domain>/
  → internal/service → internal/biz → internal/data
```
