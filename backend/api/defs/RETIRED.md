# api/defs 已退役（goctl）

> **禁止** 再使用 `make gen-api` / `goctl api`。

- **契约 SSOT**：`api/<domain>/v1/*.proto`（含 `google.api.http`）
- **生成**：`cd backend && make gen`
- **历史快照**：`scripts/archive/api-defs/`
