Name: super.rpc
ListenOn: 0.0.0.0:8080
# RPC 服务端超时（毫秒）。Moe 试跑/润色会多次调 LLM，需与 llm_inference.timeout_seconds 匹配
Timeout: 600000
# 手绘动态是否先发后审（true=moderation_status=pending，需 SQL 或管理端改为 ok）
HandDrawRequireModeration: false
#Etcd:
#  Hosts:
#  - 127.0.0.1:2379
#  Key: super.rpc
