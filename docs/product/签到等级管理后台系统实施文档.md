# 签到等级管理后台系统实施文档

## 项目现状总结

经过全面的代码分析，发现 **moe_social 项目已经具备了签到等级系统90%的基础功能**：

### ✅ 已完整实现的功能

#### 1. 数据库模型层 (100%完成)
- **UserLevel**: 用户等级表（等级、经验值、总经验）
- **LevelConfig**: 等级配置表（等级标题、所需经验、特权、徽章）
- **UserCheckIn**: 用户签到记录表（签到日期、连续天数、经验奖励）
- **CheckInReward**: 签到奖励配置表（连续天数奖励规则）
- **ExpLog**: 经验获得日志表（完整的经验变更追踪）

#### 2. 用户系统基础 (100%完成)
- **User模型已含role字段**: `user/admin/super_admin` 三种角色
- **VIP系统完整**: 套餐、订单、记录管理
- **钱包系统**: 余额管理、交易记录

#### 3. 数据库迁移 (100%完成)
- 自动迁移机制已配置，包含所有签到等级相关表
- GORM标签完整，表结构定义清晰

#### 4. 项目架构 (100%完成)
- Go-zero微服务架构成熟
- API/RPC分层清晰
- 统一的错误处理模式

### ❌ 需要新实现的功能

#### 1. API端点层 (0%完成)
目前super.api中缺少以下端点：

**用户端API (8个端点)**:
- `POST /api/user/:user_id/check-in` - 每日签到
- `GET /api/user/:user_id/level` - 获取用户等级信息
- `GET /api/user/:user_id/check-in/status` - 签到状态查询
- `GET /api/user/:user_id/check-in/history` - 签到历史
- `GET /api/user/:user_id/exp/logs` - 经验日志
- `GET /api/user/:user_id/level/progress` - 等级进度
- `GET /api/user/:user_id/level/rewards` - 可领取奖励
- `POST /api/user/:user_id/level/claim-reward` - 领取奖励

**管理员端API (10个端点)**:
- `GET /api/admin/levels` - 等级配置列表
- `POST /api/admin/levels` - 创建等级配置
- `PUT /api/admin/levels/:level_id` - 编辑等级配置
- `DELETE /api/admin/levels/:level_id` - 删除等级配置
- `GET /api/admin/check-in-rewards` - 签到奖励配置
- `POST /api/admin/check-in-rewards` - 创建奖励配置
- `PUT /api/admin/check-in-rewards/:reward_id` - 编辑奖励
- `GET /api/admin/stats/overview` - 系统统计概览
- `GET /api/admin/stats/level` - 等级统计
- `GET /api/admin/stats/check-in` - 签到统计

#### 2. 业务逻辑层 (0%完成)
- Handler层: 18个handler文件
- Logic层: 18个logic文件
- 权限检查中间件

#### 3. 前端UI (0%完成)
- 签到页面及组件
- 等级进度显示组件
- 管理后台界面

---

## 详细实施计划

### 阶段一：后端API实现 (预计5-6工作日)

#### 1.1 修改API定义文件
**文件**: `backend/api/super.api`

在文件末尾添加签到等级相关的类型定义和API端点：

```go
// 签到等级相关结构
type UserLevelInfo {
    Level       int    `json:"level"`
    Experience  int    `json:"experience"`
    TotalExp    int    `json:"total_exp"`
    NextLevelExp int   `json:"next_level_exp"`
    LevelTitle  string `json:"level_title"`
    BadgeUrl    string `json:"badge_url"`
    Progress    float64 `json:"progress"`
}

type CheckInStatus {
    HasCheckedToday    bool    `json:"has_checked_today"`
    ConsecutiveDays   int     `json:"consecutive_days"`
    TodayReward       int     `json:"today_reward"`
    NextDayReward     int     `json:"next_day_reward"`
    CanCheckIn        bool    `json:"can_check_in"`
}

type CheckInRecord {
    CheckInDate     string `json:"check_in_date"`
    ConsecutiveDays int    `json:"consecutive_days"`
    ExpReward       int    `json:"exp_reward"`
    IsSpecialReward bool   `json:"is_special_reward"`
    SpecialRewardDesc string `json:"special_reward_desc"`
}

// 请求响应结构
type CheckInReq {
    UserId string `path:"user_id"`
}

type GetUserLevelReq {
    UserId string `path:"user_id"`
}

type GetCheckInStatusReq {
    UserId string `path:"user_id"`
}

// 响应结构
type CheckInResp {
    BaseResp
    Data struct {
        ExpGained       int    `json:"exp_gained"`
        NewLevel        int    `json:"new_level"`
        ConsecutiveDays int    `json:"consecutive_days"`
        LevelUp         bool   `json:"level_up"`
        SpecialReward   string `json:"special_reward"`
    } `json:"data"`
}

type GetUserLevelResp {
    BaseResp
    Data UserLevelInfo `json:"data"`
}

type GetCheckInStatusResp {
    BaseResp
    Data CheckInStatus `json:"data"`
}
```

然后添加API服务定义：

```go
// 签到等级相关API服务
@server (
    group: checkin
)
service Super {
    @handler checkIn
    post /api/user/:user_id/check-in (CheckInReq) returns (CheckInResp)

    @handler getUserLevel
    get /api/user/:user_id/level (GetUserLevelReq) returns (GetUserLevelResp)

    @handler getCheckInStatus
    get /api/user/:user_id/check-in/status (GetCheckInStatusReq) returns (GetCheckInStatusResp)

    // 其他签到等级API...
}

// 管理员API服务 (需要权限验证)
@server (
    group: admin
    middleware: AdminAuth
)
service Super {
    // 管理员相关API端点...
}
```

#### 1.2 生成API代码
执行代码生成：
```bash
cd backend
make gen-api
```

#### 1.3 实现Handler层
**核心文件**:
- `backend/api/internal/handler/checkin/checkinhandler.go`
- `backend/api/internal/handler/checkin/getuserlevelhandler.go`
- `backend/api/internal/handler/checkin/getcheckinsstatushandler.go`

**示例实现**:
```go
// checkinhandler.go
func (l *CheckInHandler) CheckIn(req *types.CheckInReq, r *http.Request) (*types.CheckInResp, error) {
    // 调用RPC服务处理签到逻辑
    rpcReq := &super.CheckInRequest{
        UserId: req.UserId,
    }

    rpcResp, err := l.svcCtx.SuperRpc.CheckIn(l.ctx, rpcReq)
    if err != nil {
        return nil, err
    }

    return &types.CheckInResp{
        BaseResp: types.BaseResp{
            Code: 200,
            Message: "签到成功",
            Success: true,
        },
        Data: types.CheckInData{
            ExpGained: int(rpcResp.ExpGained),
            NewLevel: int(rpcResp.NewLevel),
            // ...
        },
    }, nil
}
```

#### 1.4 实现Logic层
**核心文件**:
- `backend/api/internal/logic/checkin/checkinlogic.go`
- `backend/api/internal/logic/checkin/getuserlevellogic.go`

**关键业务逻辑**:

1. **签到逻辑**:
```go
func (l *CheckInLogic) CheckIn(req *types.CheckInReq) (*types.CheckInResp, error) {
    // 1. 检查今日是否已签到
    // 2. 计算连续签到天数
    // 3. 根据连续天数确定奖励
    // 4. VIP用户经验翻倍
    // 5. 记录签到记录
    // 6. 更新用户经验和等级
    // 7. 记录经验日志
}
```

2. **等级计算逻辑**:
```go
func calculateLevel(experience int) (level int, progress float64) {
    // 根据LevelConfig表计算用户当前等级和进度
}
```

3. **经验增加逻辑**:
```go
func AddExperience(userID uint, exp int, source, sourceID, description string) error {
    // 1. 更新UserLevel表
    // 2. 记录ExpLog
    // 3. 检查是否升级
    // 4. 发送升级通知
}
```

#### 1.5 实现权限中间件
**文件**: `backend/api/internal/middleware/adminauth.go`

```go
func AdminAuth() rest.Middleware {
    return func(next http.HandlerFunc) http.HandlerFunc {
        return func(w http.ResponseWriter, r *http.Request) {
            // 1. 从JWT中获取用户ID
            // 2. 查询用户role字段
            // 3. 验证是否为admin或super_admin
            // 4. 记录管理操作日志
        }
    }
}
```

### 阶段二：RPC服务实现 (预计3工作日)

#### 2.1 修改RPC定义
**文件**: `backend/rpc/super.proto`

添加签到等级相关的message和service定义

#### 2.2 生成RPC代码
```bash
cd backend
make gen-rpc
```

#### 2.3 实现RPC Logic
实现核心的签到、等级计算等业务逻辑

### 阶段三：前端UI实现 (预计6-8工作日)

#### 3.1 数据模型层
**文件**: `lib/models/user_level.dart`

```dart
class UserLevel {
  final int level;
  final int experience;
  final int totalExp;
  final int nextLevelExp;
  final String levelTitle;
  final String badgeUrl;
  final double progress;

  // fromJson, toJson, copyWith等方法
}

class CheckInStatus {
  final bool hasCheckedToday;
  final int consecutiveDays;
  final int todayReward;
  final bool canCheckIn;

  // ...
}
```

#### 3.2 服务层
**文件**: `lib/services/gamification_service.dart`

```dart
class GamificationService {
  static Future<UserLevel> getUserLevel(String userId) async {
    // 调用API获取用户等级信息
  }

  static Future<CheckInResult> checkIn(String userId) async {
    // 调用签到API
  }

  static Future<CheckInStatus> getCheckInStatus(String userId) async {
    // 获取签到状态
  }
}
```

#### 3.3 签到页面
**文件**: `lib/pages/check_in_page.dart`

萌系设计风格的签到页面：
- 柔和的#7F7FD5主色调
- 24px圆角设计
- FadeInUp入场动画
- 签到日历显示
- 奖励预览
- 签到按钮（大圆角Stadium边框）

#### 3.4 等级进度组件
**文件**: `lib/widgets/level_progress_bar.dart`

```dart
class LevelProgressBar extends StatelessWidget {
  final UserLevel userLevel;
  final bool showAnimation;

  // 圆角进度条，萌系配色
  // 支持升级动画效果
}
```

#### 3.5 个人资料页面集成
**文件**: `lib/pages/profile_page.dart` (修改)

在个人资料页面添加：
- 用户等级徽章显示
- 经验进度条
- 签到状态提示
- 快捷签到按钮

### 阶段四：管理后台实现 (预计5-7工作日)

#### 4.1 管理后台数据服务
**文件**: `lib/services/admin_service.dart`

```dart
class AdminService {
  static Future<List<LevelConfig>> getLevelConfigs() async {}
  static Future<LevelConfig> createLevelConfig(LevelConfig config) async {}
  static Future<LevelConfig> updateLevelConfig(LevelConfig config) async {}
  // ...
}
```

#### 4.2 管理后台页面
**文件**: `lib/pages/admin/admin_dashboard_page.dart`

管理后台首页：
- 系统统计概览
- 快捷管理入口
- 数据图表展示

**文件**: `lib/pages/admin/level_management_page.dart`

等级配置管理：
- 等级列表展示
- 创建/编辑等级配置表单
- 批量操作功能

#### 4.3 权限控制
在路由和页面级别添加管理员权限检查

---

## 关键实现细节

### 1. 签到逻辑设计

```go
// 签到核心算法
func ProcessCheckIn(userID uint) (*CheckInResult, error) {
    // 1. 检查今日签到状态
    today := time.Now().Format("2006-01-02")
    lastCheckIn := getUserLastCheckIn(userID)

    if lastCheckIn != nil && lastCheckIn.CheckInDate.Format("2006-01-02") == today {
        return nil, errors.New("今日已签到")
    }

    // 2. 计算连续天数
    consecutiveDays := 1
    if lastCheckIn != nil {
        yesterday := time.Now().AddDate(0, 0, -1).Format("2006-01-02")
        if lastCheckIn.CheckInDate.Format("2006-01-02") == yesterday {
            consecutiveDays = lastCheckIn.ConsecutiveDays + 1
        }
    }

    // 3. 确定奖励
    baseExp := 10
    reward := getCheckInReward(consecutiveDays)
    totalExp := baseExp + reward.ExpReward

    // 4. VIP加成
    user := getUserByID(userID)
    if user.IsVip {
        totalExp *= 2
    }

    // 5. 保存签到记录
    checkInRecord := &UserCheckIn{
        UserID: userID,
        CheckInDate: time.Now(),
        ConsecutiveDays: consecutiveDays,
        ExpReward: totalExp,
    }
    saveCheckInRecord(checkInRecord)

    // 6. 增加经验
    oldLevel := getUserLevel(userID)
    addExperience(userID, totalExp, "check_in", "", "每日签到")
    newLevel := getUserLevel(userID)

    return &CheckInResult{
        ExpGained: totalExp,
        ConsecutiveDays: consecutiveDays,
        LevelUp: newLevel.Level > oldLevel.Level,
        NewLevel: newLevel.Level,
    }, nil
}
```

### 2. 等级系统设计

**默认等级配置**:
```go
var defaultLevels = []LevelConfig{
    {Level: 1, Title: "萌新菜鸟", MinExp: 0, MaxExp: 100, BadgeUrl: "/badges/level1.png"},
    {Level: 2, Title: "活跃新手", MinExp: 100, MaxExp: 500, BadgeUrl: "/badges/level2.png"},
    {Level: 3, Title: "社区中坚", MinExp: 500, MaxExp: 2000, BadgeUrl: "/badges/level3.png"},
    {Level: 4, Title: "资深达人", MinExp: 2000, MaxExp: 5000, BadgeUrl: "/badges/level4.png"},
    {Level: 5, Title: "社区大师", MinExp: 5000, MaxExp: 999999, BadgeUrl: "/badges/level5.png"},
}
```

**等级特权配置**:
```json
{
  "daily_post_limit": 10,
  "daily_comment_limit": 50,
  "can_use_premium_emoji": true,
  "storage_quota_gb": 2,
  "exclusive_features": ["priority_support", "exclusive_frames"]
}
```

### 3. 经验来源设计

```go
const (
    ExpSourceCheckIn     = "check_in"      // 签到: 10经验
    ExpSourcePost        = "post"          // 发帖: 5经验
    ExpSourceComment     = "comment"       // 评论: 2经验
    ExpSourceLikeReceived = "like_received" // 获赞: 1经验
    ExpSourceVipBonus    = "vip_bonus"     // VIP加成
    ExpSourceTaskComplete = "task_complete" // 任务完成
    ExpSourceEventReward = "event_reward"  // 活动奖励
)

// VIP用户经验加成规则
func applyVipBonus(baseExp int, isVip bool) int {
    if isVip {
        return int(float64(baseExp) * 1.5) // VIP用户1.5倍经验
    }
    return baseExp
}
```

---

## 数据库初始化脚本

### 等级配置初始化
**文件**: `backend/scripts/init_level_configs.go`

```go
func InitLevelConfigs() error {
    configs := []model.LevelConfig{
        {Level: 1, Title: "萌新菜鸟", MinExp: 0, MaxExp: 100},
        {Level: 2, Title: "活跃新手", MinExp: 100, MaxExp: 500},
        {Level: 3, Title: "社区中坚", MinExp: 500, MaxExp: 2000},
        {Level: 4, Title: "资深达人", MinExp: 2000, MaxExp: 5000},
        {Level: 5, Title: "社区大师", MinExp: 5000, MaxExp: 999999},
    }

    for _, config := range configs {
        utils.DB.FirstOrCreate(&config, model.LevelConfig{Level: config.Level})
    }
    return nil
}
```

### 签到奖励配置初始化
```go
func InitCheckInRewards() error {
    rewards := []model.CheckInReward{
        {ConsecutiveDays: 1, ExpReward: 0},    // 基础签到无额外奖励
        {ConsecutiveDays: 3, ExpReward: 5},    // 连续3天+5经验
        {ConsecutiveDays: 7, ExpReward: 20},   // 连续7天+20经验
        {ConsecutiveDays: 15, ExpReward: 50},  // 连续15天+50经验
        {ConsecutiveDays: 30, ExpReward: 100}, // 连续30天+100经验
    }

    for _, reward := range rewards {
        utils.DB.FirstOrCreate(&reward, model.CheckInReward{ConsecutiveDays: reward.ConsecutiveDays})
    }
    return nil
}
```

---

## 测试计划

### 1. 单元测试
- 签到逻辑测试（连续天数、重复签到、VIP加成）
- 等级计算测试
- 经验增加测试
- 权限验证测试

### 2. 集成测试
- API端点测试（使用Postman/curl）
- 数据库事务测试
- 并发签到测试

### 3. UI测试
- 不同屏幕尺寸适配
- 动画流畅性测试
- 用户交互测试

---

## 部署和上线

### 1. 数据库迁移
由于GORM自动迁移机制，新表会在应用启动时自动创建

### 2. 初始数据导入
运行等级配置和签到奖励的初始化脚本

### 3. 渐进式上线
1. 后端API先行部署
2. 前端功能逐步发布
3. 管理后台最后上线

---

## 预期工作量总结

| 阶段 | 工作内容 | 预计工作日 | 负责人建议 |
|------|----------|------------|------------|
| 后端API | API定义、Handler、Logic、权限 | 5-6天 | 后端开发 |
| RPC服务 | RPC定义、业务逻辑实现 | 3天 | 后端开发 |
| 前端UI | 签到页面、等级组件、集成 | 6-8天 | 前端开发 |
| 管理后台 | 管理界面、权限控制 | 5-7天 | 全栈开发 |
| 测试调试 | 单元测试、集成测试、UI测试 | 3-4天 | 全团队 |

**总计**: 22-28个工作日 (约4-5.5周)

---

## 关键风险点与解决方案

### 1. 并发签到问题
**风险**: 多请求同时签到导致数据不一致
**解决**: 使用数据库事务和乐观锁

### 2. 等级计算性能
**风险**: 大量用户等级计算影响性能
**解决**: 实现Redis缓存，异步更新等级

### 3. VIP加成逻辑
**风险**: VIP状态判断和经验加成可能不准确
**解决**: 在经验增加时实时查询VIP状态

### 4. 管理员权限控制
**风险**: 权限验证不严格导致安全问题
**解决**: 多层权限验证，操作日志完整记录

---

## 结论

基于现有的moe_social项目代码，**签到等级管理后台系统的实现相对简单**，因为：

1. ✅ **数据模型层已100%完成** - 无需修改数据库结构
2. ✅ **基础架构已成熟** - 按现有模式扩展即可
3. ✅ **VIP和用户系统已完整** - 可直接集成
4. 🔄 **主要工作集中在API和UI层** - 工作量可控且风险较低

**建议优先级**:
1. 先实现用户端的签到和等级功能（核心价值）
2. 再实现管理后台（运营需求）
3. 最后优化UI和用户体验

这样可以快速上线核心功能，然后逐步完善管理和运营工具。