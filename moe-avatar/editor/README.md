# Moe Avatar Editor

Standalone editor shell for the open-source avatar core.

Dev:

```bash
cd moe-avatar/editor
npm install
npm run dev
```

It reads the shared `moe-admin/public/pet/moe_avatar` pack during development.
