# 项目名称替换说明

## ✅ 已修改的文件

### 1. 前端应用名称
- ✅ `lib/main.dart` 
  - 第26行：`title: 'Simple Auth App'` → `title: 'Moe Social'`
  - 第209行：`'欢迎使用 Simple Auth'` → `'欢迎使用 Moe Social'`

### 2. Web配置
- ✅ `web/index.html` - 已正确设置为 "Moe Social"
- ✅ `web/manifest.json` - 已正确设置为 "Moe Social"

### 3. Android配置
- ✅ `android/app/src/main/AndroidManifest.xml` - 已正确设置为 "Moe Social"

## ⚠️ 其他平台配置文件（可选修改）

以下文件包含旧的项目名称，但这些是内部标识符，不影响用户看到的显示名称：

### Windows
- `windows/runner/Runner.rc` - 内部文件名
- `windows/CMakeLists.txt` - 构建配置

### macOS
- `macos/Runner.xcodeproj/` - Xcode项目配置
- `macos/Runner/Configs/AppInfo.xcconfig` - 内部标识符

### Linux
- `linux/runner/my_application.cc` - 窗口标题
- `linux/CMakeLists.txt` - 构建配置

### iOS
- `ios/Runner.xcodeproj/project.pbxproj` - Xcode项目配置

### Android
- `android/app/src/main/kotlin/com/example/simple_auth_app/MainActivity.kt` - 包名

## 📝 建议

### 如果只开发Web应用
- ✅ 当前修改已足够，Web应用会显示正确的名称

### 如果需要发布到其他平台
- 可以考虑修改包名和内部标识符，但这需要更多配置
- 当前用户可见的名称已经正确

## 🎯 测试

重新运行前端应用，浏览器标签页应该显示 "Moe Social" 而不是 "Simple Auth App"。

