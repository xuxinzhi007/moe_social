# 常见问题

## 项目特有问题（必读）

这些是 Moe Social 项目本身代码中已经确认的坑，开发时直接对照处理。

---

### 1. MoeToast 弹窗不显示

**症状**：调用了 `MoeToast.success/error` 或 `loadingProvider.setSuccess/setError`，但没有任何提示出现，控制台可能有 `MoeToast: No Overlay found` 日志。

**根本原因**：`MoeToast` 依赖 `Overlay.maybeOf(context)` 找 Overlay。以下两种 context 处于 Navigator Overlay **上方**，会返回 null：
- `AuthService.navigatorKey.currentContext`（Navigator 自身的 context）
- `MaterialApp.builder` 里的 context（`AppMessageWidget` 所在位置）

**解法**：始终用**页面内的 `context`** 调用 MoeToast，在导航跳转**之前**调用：
```dart
MoeToast.success(context, '操作成功');       // 先 toast
Navigator.pushReplacementNamed(context, '/home'); // 后跳转
```

---

### 2. executeOperation 的 onSuccess 被调用，但实际上操作失败

**症状**：登录 / 注册失败（密码错误等），但 `onSuccess` 回调仍然执行，导致错误地跳转或显示成功提示。

**根本原因**：`AuthService.login/register` 内部 catch 了 `ApiException` 并封装成 `AuthResult.failure(...)`，不抛出。`executeOperation` 认为 Future 正常完成，调用 `onSuccess`。

**解法**：`onSuccess` 里必须检查 `result.success`：
```dart
onSuccess: (result) {
  if (!result.success) {
    MoeToast.error(context, result.errorMessage ?? '操作失败');
    return;
  }
  // 真正成功才往下走
}
```

---

### 3. 登录后通知/消息不更新

**症状**：登录成功后未读消息数不显示，WebSocket 没有连接，push 通知不响应。

**根本原因**：`NotificationProvider.init()` 没有在登录成功后调用。

**解法**：
```dart
onSuccess: (result) {
  if (!result.success) { ... return; }
  try { context.read<NotificationProvider>().init(); } catch (_) {}
  // 再导航
}
```

---

### 4. 点赞状态在切换页面后丢失或反转

**症状**：从帖子列表点赞，切换页面后点赞状态消失或与实际相反。

**根本原因**：服务端 List 接口返回的 `isLiked` 字段不可靠（已知 bug），实际状态需要从本地 `LikeStateManager` + `AuthService.getLikeStatus` 合并。

**解法**：不要直接信任 API 返回的 `isLiked`，通过 `PostService.getPosts` 等已有方法获取帖子，内部已处理合并逻辑。

---

### 5. Web 端刷新后路由参数丢失

**症状**：Web 端刷新 `/user-profile` 或 `/direct-chat` 页面，出现空白或崩溃。

**根本原因**：Web 端刷新后 `ModalRoute.of(context)?.settings.arguments` 为 null，直接 `!` 断言会崩溃。

**解法**：有参数的路由必须做防御性检查（项目中已对 `/user-profile`、`/direct-chat` 做了）：
```dart
'/user-profile': (context) {
  final args = ModalRoute.of(context)?.settings.arguments;
  if (args is! Map<String, dynamic>) {
    return const Scaffold(body: Center(child: Text('页面参数丢失，请返回首页重新进入')));
  }
  return UserProfilePage(...);
},
```

---

### 6. 未完成的后端功能（TODO）

以下功能前端 UI 已存在，但后端接口尚未实现，开发时注意不要误以为是前端 bug：

- **举报帖子**：`post_card.dart` 中只弹 Snackbar，实际接口调用注释为 `TODO: 调用后端举报接口`
- **按话题筛选帖子**：`topic_posts_page.dart` 中注释 `TODO: 后端支持按话题ID筛选`
- **话题历史/推荐**：`topic_tag.dart` 中有两处 TODO 占位

---

## 概述

在Moe Social项目开发和使用过程中，可能会遇到各种问题。本指南将详细介绍常见问题及其解决方案，帮助开发者和用户快速解决问题。

## 前端常见问题

### 1. Flutter构建失败

#### 问题描述
Flutter构建失败，出现各种错误。

#### 可能原因
- 依赖问题：依赖版本不兼容
- 配置问题：Flutter配置不正确
- 网络问题：无法下载依赖
- 权限问题：缺少必要的权限

#### 解决方案
- **依赖问题**：
  ```bash
  flutter pub get
  flutter pub upgrade
  ```

- **配置问题**：
  ```bash
  flutter doctor
  # 根据输出修复配置问题
  ```

- **网络问题**：
  - 检查网络连接
  - 配置镜像源
  ```bash
  # 在flutterinlutter.bat中添加
  set PUB_HOSTED_URL=https://pub.flutter-io.cn
  set FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn
  ```

- **权限问题**：
  - 确保有足够的权限访问项目目录
  - 以管理员身份运行命令

### 2. 应用启动白屏

#### 问题描述
应用启动后显示白屏，无法正常加载。

#### 可能原因
- 资源加载问题：资源文件丢失或路径错误
- 初始化问题：应用初始化失败
- 网络问题：网络请求失败
- 权限问题：缺少必要的权限

#### 解决方案
- **资源加载问题**：
  - 检查资源文件是否存在
  - 检查资源路径是否正确
  - 运行 `flutter clean` 后重新构建

- **初始化问题**：
  - 检查应用初始化代码
  - 添加日志，定位初始化失败的原因
  - 确保所有必要的服务都已正确初始化

- **网络问题**：
  - 检查网络连接
  - 检查API地址是否正确
  - 实现网络错误处理

- **权限问题**：
  - 检查应用权限配置
  - 确保在运行时请求必要的权限

### 3. 布局问题

#### 问题描述
应用布局不正确，出现UI错乱。

#### 可能原因
- 布局约束问题：Widget约束不正确
- 响应式设计问题：未考虑不同屏幕尺寸
- 状态管理问题：状态更新导致布局异常
- 动画问题：动画导致布局不稳定

#### 解决方案
- **布局约束问题**：
  - 使用正确的布局Widget，如Container、Row、Column等
  - 确保Widget有正确的约束
  - 使用Expanded、Flexible等Widget调整布局

- **响应式设计问题**：
  - 使用MediaQuery获取屏幕尺寸
  - 使用LayoutBuilder适应不同尺寸
  - 考虑使用ResponsiveBuilder等第三方库

- **状态管理问题**：
  - 检查状态更新逻辑
  - 确保状态更新不会导致无限重建
  - 使用const构造器减少不必要的重建

- **动画问题**：
  - 确保动画不会影响布局稳定性
  - 使用AnimatedContainer等内置动画Widget
  - 避免在布局构建过程中执行动画

### 4. 性能问题

#### 问题描述
应用运行卡顿，性能不佳。

#### 可能原因
- 渲染问题：Widget重建频繁
- 计算问题：执行耗时操作
- 内存问题：内存泄漏
- 网络问题：网络请求过多

#### 解决方案
- **渲染问题**：
  - 使用const构造器
  - 避免在build方法中执行耗时操作
  - 使用RepaintBoundary减少重绘
  - 使用ListView.builder等懒加载Widget

- **计算问题**：
  - 将耗时操作移到异步线程
  - 使用compute函数执行耗时计算
  - 缓存计算结果

- **内存问题**：
  - 及时释放资源
  - 避免内存泄漏
  - 使用WeakReference管理大型对象
  - 定期检查内存使用情况

- **网络问题**：
  - 合并网络请求
  - 实现网络缓存
  - 使用批量请求减少网络调用

## 后端常见问题

### 1. Go编译失败

#### 问题描述
Go代码编译失败，出现各种错误。

#### 可能原因
- 依赖问题：依赖版本不兼容
- 语法问题：代码语法错误
- 配置问题：Go环境配置不正确
- 路径问题：导入路径错误

#### 解决方案
- **依赖问题**：
  ```bash
  go mod tidy
  go mod download
  ```

- **语法问题**：
  - 检查代码语法
  - 使用 `go fmt` 格式化代码
  - 使用 `go vet` 检查代码

- **配置问题**：
  ```bash
  go version
  # 确保Go版本正确
  ```

- **路径问题**：
  - 检查导入路径
  - 确保模块路径正确
  - 使用 `go mod edit -replace` 解决路径问题

### 2. API调用失败

#### 问题描述
API调用失败，返回错误。

#### 可能原因
- 网络问题：无法连接到服务器
- 认证问题：认证失败
- 参数问题：参数错误
- 服务器问题：服务器内部错误

#### 解决方案
- **网络问题**：
  - 检查网络连接
  - 检查服务器地址是否正确
  - 检查防火墙设置

- **认证问题**：
  - 检查认证令牌是否有效
  - 检查权限是否正确
  - 重新登录获取新的令牌

- **参数问题**：
  - 检查API参数是否正确
  - 检查参数类型是否匹配
  - 检查参数格式是否符合要求

- **服务器问题**：
  - 检查服务器日志
  - 检查数据库连接
  - 重启服务器

### 3. 数据库连接问题

#### 问题描述
无法连接到数据库，出现连接错误。

#### 可能原因
- 数据库服务未运行
- 连接参数错误
- 网络问题
- 权限问题

#### 解决方案
- **数据库服务未运行**：
  - 检查数据库服务是否运行
  - 启动数据库服务

- **连接参数错误**：
  - 检查数据库连接字符串
  - 检查用户名和密码
  - 检查数据库名称

- **网络问题**：
  - 检查网络连接
  - 检查数据库服务器地址
  - 检查防火墙设置

- **权限问题**：
  - 检查数据库用户权限
  - 确保用户有足够的权限

### 4. 性能问题

#### 问题描述
后端服务性能不佳，响应缓慢。

#### 可能原因
- 数据库查询问题：查询效率低
- 代码问题：代码执行效率低
- 资源问题：服务器资源不足
- 并发问题：并发处理不当

#### 解决方案
- **数据库查询问题**：
  - 优化SQL查询
  - 添加索引
  - 使用缓存
  - 减少数据库查询次数

- **代码问题**：
  - 优化代码逻辑
  - 减少不必要的计算
  - 使用更高效的算法
  - 避免重复代码

- **资源问题**：
  - 增加服务器资源
  - 优化服务器配置
  - 使用负载均衡

- **并发问题**：
  - 优化并发处理
  - 使用连接池
  - 避免死锁
  - 合理使用锁机制

## 部署常见问题

### 1. 构建失败

#### 问题描述
应用构建失败，无法生成可执行文件。

#### 可能原因
- 依赖问题：依赖安装失败
- 配置问题：构建配置错误
- 权限问题：缺少构建权限
- 资源问题：磁盘空间不足

#### 解决方案
- **依赖问题**：
  - 确保所有依赖都已安装
  - 检查依赖版本兼容性
  - 清理缓存后重新安装依赖

- **配置问题**：
  - 检查构建配置文件
  - 确保构建参数正确
  - 检查环境变量设置

- **权限问题**：
  - 确保有足够的权限执行构建
  - 以管理员身份运行构建命令

- **资源问题**：
  - 检查磁盘空间
  - 清理临时文件
  - 增加构建服务器资源

### 2. 部署失败

#### 问题描述
应用部署失败，无法正常运行。

#### 可能原因
- 环境问题：部署环境不兼容
- 配置问题：部署配置错误
- 权限问题：缺少运行权限
- 端口问题：端口被占用

#### 解决方案
- **环境问题**：
  - 确保部署环境满足应用要求
  - 检查操作系统版本
  - 检查依赖库版本

- **配置问题**：
  - 检查部署配置文件
  - 确保配置参数正确
  - 检查环境变量设置

- **权限问题**：
  - 确保应用有足够的运行权限
  - 检查文件和目录权限
  - 以正确的用户身份运行应用

- **端口问题**：
  - 检查端口是否被占用
  - 更改应用端口
  - 关闭占用端口的进程

### 3. 应用启动失败

#### 问题描述
应用部署后无法启动，出现启动错误。

#### 可能原因
- 配置问题：配置错误
- 依赖问题：缺少依赖
- 权限问题：缺少权限
- 资源问题：资源不足

#### 解决方案
- **配置问题**：
  - 检查应用配置文件
  - 确保配置参数正确
  - 检查环境变量设置

- **依赖问题**：
  - 确保所有依赖都已安装
  - 检查依赖版本兼容性
  - 重新安装依赖

- **权限问题**：
  - 确保应用有足够的运行权限
  - 检查文件和目录权限
  - 以正确的用户身份运行应用

- **资源问题**：
  - 检查服务器资源使用情况
  - 增加服务器资源
  - 优化应用资源使用

## 安全问题

### 1. 认证问题

#### 问题描述
用户认证失败，无法登录。

#### 可能原因
- 密码错误：用户输入错误的密码
- 账户问题：账户被锁定或禁用
- 令牌问题：认证令牌过期或无效
- 服务器问题：认证服务故障

#### 解决方案
- **密码错误**：
  - 确认密码输入正确
  - 使用忘记密码功能重置密码

- **账户问题**：
  - 检查账户是否被锁定
  - 联系管理员解锁账户

- **令牌问题**：
  - 重新登录获取新令牌
  - 检查令牌有效期设置

- **服务器问题**：
  - 检查认证服务是否正常运行
  - 检查数据库连接

### 2. 授权问题

#### 问题描述
用户无法访问某些功能，出现授权错误。

#### 可能原因
- 权限不足：用户权限不足
- 角色问题：用户角色不正确
- 配置问题：权限配置错误
- 服务器问题：授权服务故障

#### 解决方案
- **权限不足**：
  - 联系管理员提升权限
  - 检查用户角色是否正确

- **角色问题**：
  - 检查用户角色配置
  - 确保角色有正确的权限

- **配置问题**：
  - 检查权限配置文件
  - 确保权限规则正确

- **服务器问题**：
  - 检查授权服务是否正常运行
  - 检查数据库连接

### 3. 安全漏洞

#### 问题描述
应用存在安全漏洞，可能被攻击。

#### 可能原因
- 代码问题：代码存在安全漏洞
- 依赖问题：依赖库存在安全漏洞
- 配置问题：安全配置错误
- 环境问题：运行环境不安全

#### 解决方案
- **代码问题**：
  - 修复代码中的安全漏洞
  - 进行安全代码审查
  - 使用安全扫描工具检查代码

- **依赖问题**：
  - 更新依赖库到安全版本
  - 使用依赖扫描工具检查漏洞
  - 定期更新依赖库

- **配置问题**：
  - 检查安全配置
  - 确保使用安全的配置参数
  - 遵循安全最佳实践

- **环境问题**：
  - 确保运行环境安全
  - 配置防火墙和入侵检测系统
  - 定期安全审计

## 总结

本指南介绍了Moe Social项目中常见的问题及其解决方案，涵盖了前端、后端、部署和安全等多个方面。希望这些信息能够帮助开发者和用户快速解决问题，确保项目的顺利运行。

在实际开发和使用过程中，遇到问题时应首先分析问题的根本原因，然后采取相应的解决方案。如果问题无法解决，可以参考相关文档或寻求社区的帮助。